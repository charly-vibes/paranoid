pre-commit:
# ah:managed:start
  ah-check:
    run: ah check
# ah:managed:end

  commands:
    shell:
      run: bash -n justfile

pre-push:
  commands:
    structure:
      run: test -f android/gradlew
