Implemented PARANOID-7mh by adding UsageStats-backed interval reading and slice mapping, plus battery snapshot collection runtime hooks (app-open, power-change, boot) and persistence seams. Rule-of-5 review flagged lifecycle overcapture risk and deprecated event constants; lifecycle hooks were kept minimal for v1 tests and deprecation warnings were isolated to the Android reader boundary.
