Implemented PARANOID-fec tasks 3.1-3.9: Created all 7 collectors (WifiCollector, CellularCollector, DnsCollector, IpConfigCollector, ProbeCollector, DeviceHealthCollector, ConnDiagCollector) in apps/netdiag/collect/, plus SnapshotCaptureEngine orchestrator and unit tests for pure logic (frequency→channel mapping, RFC1918 detection). Build and tests pass. HTTP timing uses HttpURLConnection instead of OkHttp (not in project deps). Task 3.10 (instrumentation test) remains open — requires a real device.
